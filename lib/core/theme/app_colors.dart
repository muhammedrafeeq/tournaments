import 'package:flutter/material.dart';

class AppColors {
  // ── Brand ──────────────────────────────────────────────────────────────
  static const primary = Color(0xFF00E676);
  static const primaryDim = Color(0xFF00C853);
  static const primaryDeep = Color(0xFF00963E);
  static const primaryGlow = Color(0x3300E676);
  static const primarySurface = Color(0x1200E676);

  // ── Backgrounds ────────────────────────────────────────────────────────
  static const bg = Color(0xFF070B09);
  static const bgSurface = Color(0xFF0D1410);
  static const bgCard = Color(0xFF111A14);
  static const bgElevated = Color(0xFF182219);

  // ── Text ───────────────────────────────────────────────────────────────
  static const textPrimary = Color(0xFFF0FFF4);
  static const textSecondary = Color(0xFFB8D4C4); // lifted: was #8BA898
  static const textTertiary = Color(0xFF7A9E8A); // lifted: was #4D6B58
  static const textInverse = Color(0xFF070B09);

  // ── Accents ────────────────────────────────────────────────────────────
  static const gold = Color(0xFFFFD600);
  static const goldDim = Color(0xFFB39700);
  static const danger = Color(0xFFFF1744);
  static const dangerDim = Color(0xFFB71C1C);
  static const warning = Color(0xFFFF6D00);
  static const info = Color(0xFF00B0FF);

  // ── Live pulse ─────────────────────────────────────────────────────────
  static const live = Color(0xFFFF1744);
  static const liveGlow = Color(0x33FF1744);

  // ── Glass surfaces ─────────────────────────────────────────────────────
  static const glassFill = Color(0x0DFFFFFF);
  static const glassBorder = Color(0x1A00E676);
  static const glassShine = Color(0x08FFFFFF);

  // ── Dividers / strokes ─────────────────────────────────────────────────
  static const divider = Color(0xFF1E3025);
  static const stroke = Color(0xFF263D2E);   // lifted: was #1E3025
  static const strokeBright = Color(0xFF345443); // lifted: was #2A4535
}
